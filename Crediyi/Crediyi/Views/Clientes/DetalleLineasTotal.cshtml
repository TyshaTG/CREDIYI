@model IEnumerable<Crediyi.Models.LineaCabezal>

@{
    ViewBag.Title = "DetalleLineasTotal";
}

<h2>Detalle Lineas Total</h2>

<p>
    @Html.ActionLink("Inicio ", "MenuPrincipal", "Home") 

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdLinea)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Productos.DscProd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImpLinea)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdLinea)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Productos.DscProd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImpLinea)
            </td>
            <td></td>
        </tr>
    }
    

</table>
